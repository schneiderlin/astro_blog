---
import GoogleAnalytic from '@components/GoogleAnalytic.astro';
import OpenGraphTags from '@components/OpenGraphTags.astro';

interface Props {
	title: string;
	description: string;
	lang?: string;
	ogImage?: string;
}

const { title, description, lang = 'en', ogImage = '/og.jpeg' } = Astro.props;
---

<!doctype html>
<html lang={lang} class="overflow-x-hidden">
	<head>
		<GoogleAnalytic />
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="sitemap" href="/sitemap-index.xml" />
		<meta name="generator" content={Astro.generator} />

		<!-- Open Graph, for social media share preview -->
		<OpenGraphTags title={title} description={description} ogImage={ogImage} />
		<title>{title}</title>
	</head>
	<body class="overflow-x-hidden" style="background-color: #f0f0f0;">
		<slot />
	</body>
</html>
